/**
 * Microsoft OAuth Callback Page
 * Handles OAuth callback specifically for Microsoft Calendar integration
 */

import { CalendarCallbackContainer } from '@/containers/settings/CalendarCallbackContainer'

export default function MicrosoftOAuthCallbackPage() {
  return <CalendarCallbackContainer />
}
